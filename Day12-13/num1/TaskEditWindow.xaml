<Window x:Class="num1.TaskEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование задачи" Height="350" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Название:" 
               VerticalAlignment="Center" FontWeight="Bold"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="TitleTextBox" 
                 Margin="5,0,0,5" Height="30" FontSize="14"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Описание:" 
               VerticalAlignment="Top" Margin="0,10,0,0" FontWeight="Bold"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="DescriptionTextBox" 
                 Height="120" TextWrapping="Wrap" AcceptsReturn="True" 
                 VerticalScrollBarVisibility="Auto" Margin="5,10,0,5"/>

        <Border Grid.Row="3" Grid.Column="1" Margin="5,10,0,0" 
                Background="#FFF0F0F0" BorderBrush="#FFCCCCCC" 
                BorderThickness="1" Padding="5">
            <TextBlock x:Name="ValidationMessage" 
                       Foreground="Red" FontSize="11"
                       Text="* Название не может быть пустым"
                       Visibility="Collapsed"/>
        </Border>

        <StackPanel Grid.Row="4" Grid.Column="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,15,0,0">
            <Button x:Name="SaveButton" Content="Сохранить" 
                    Width="100" Height="30" Margin="0,0,10,0"
                    Background="#FF4CAF50" Foreground="White"
                    FontWeight="Bold" Click="SaveButton_Click"/>
            <Button x:Name="CancelButton" Content="Отмена" 
                    Width="100" Height="30"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>