<Window x:Class="num1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:num1"
        mc:Ignorable="d"
        Title="Управление задачами (Task Manager)" Height="450" Width="800">

    <Window.DataContext>
        <local:TaskManagerViewModel />
    </Window.DataContext>

    <Window.InputBindings>
        <KeyBinding Key="N" Modifiers="Ctrl" Command="{Binding AddTaskCommand}"/>
        <KeyBinding Key="E" Modifiers="Ctrl" Command="{Binding EditTaskCommand}"/>
        <KeyBinding Key="D" Modifiers="Ctrl" Command="{Binding DeleteTaskCommand}"/>
    </Window.InputBindings>

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Файл">
                <MenuItem Header="Выход" InputGestureText="Alt+F4" Click="ExitMenuItem_Click"/>
            </MenuItem>
            <MenuItem Header="Задачи">
                <MenuItem Header="Создать задачу" 
                          Command="{Binding AddTaskCommand}" 
                          InputGestureText="Ctrl+N"/>
                <MenuItem Header="Редактировать" 
                          Command="{Binding EditTaskCommand}" 
                          InputGestureText="Ctrl+E"/>
                <Separator />
                <MenuItem Header="Удалить" 
                          Command="{Binding DeleteTaskCommand}" 
                          InputGestureText="Ctrl+D"/>
            </MenuItem>
            <MenuItem Header="Настройки" />
        </Menu>

        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Command="{Binding AddTaskCommand}" ToolTip="Создать задачу (Ctrl+N)">
                    <TextBlock Text="➕Добавить" Margin="2"/>
                </Button>
                <Button Command="{Binding EditTaskCommand}" ToolTip="Редактировать задачу (Ctrl+E)">
                    <TextBlock Text="✏️Редактировать" Margin="2"/>
                </Button>
                <Separator />
                <Button Command="{Binding DeleteTaskCommand}" ToolTip="Удалить задачу (Ctrl+D)">
                    <TextBlock Text="❌Удалить" Margin="2"/>
                </Button>
            </ToolBar>
        </ToolBarTray>

        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem Content="Готово"/>
        </StatusBar>

        <Grid Margin="10">
            <DataGrid Name="TasksDataGrid"
                      ItemsSource="{Binding Tasks}"
                      SelectedItem="{Binding SelectedTask, UpdateSourceTrigger=PropertyChanged}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True">

                <DataGrid.Resources>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Height" Value="35"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Дата" 
                                        Binding="{Binding CreatedDate, StringFormat='dd.MM.yyyy HH:mm'}" 
                                        Width="120"/>
                    <DataGridTextColumn Header="Название" 
                                        Binding="{Binding Title}" 
                                        Width="200"/>
                    <DataGridTextColumn Header="Описание" 
                                        Binding="{Binding Description}" 
                                        Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </DockPanel>
</Window>